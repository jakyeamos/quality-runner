# Task pilot hardening

## Outcome

Make task-scoped Quality Runner evidence safe for a nonblocking development
pilot by removing equivalent-worktree toolchain drift and exposing an explicit,
fail-closed release predicate.

## Baseline

- Branch base: `e283d536242a0398db44df394902ba99932594bd`
- Task id: `pilot-hardening-20260827`
- Baseline run: `20260827T150901Z-027d61d1-task-start`
- Coverage: code quality complete; security complete
- Locked QR doctor: ready, version 0.7.0

## Batches

1. Normalize repository-local command paths before hashing toolchain identity.
   Preserve distinct absolute identities for external commands.
2. Add `qr task release-check`, authoritative-only and exit-zero only when
   `release_readiness.eligible` is true.
3. Update schemas, CLI and contract tests, agent/operator documentation,
   nonblocking CI pilot wiring, and the change-surface matrix.
4. Run focused checks, broad repository validation, authoritative QR evidence,
   then commit and push the isolated branch.

## Boundaries

- Do not alter the dirty primary checkout.
- Do not weaken, waive, or rebaseline around a failed required gate.
- Keep the CI pilot nonblocking.
- Do not merge, tag, publish, or install this branch.
- The existing `quality_runner/fleet/audit.py` large-file failure belongs to
  another active branch and remains an explicit broad-suite disposition here.

## Verification

- Positive: equivalent repository-local tool paths hash identically across
  worktrees; a clean release-check exits zero and reports eligible.
- Negative: different relative or external tool paths remain distinct; a new
  enforced finding makes release-check nonzero.
- Boundary: fast checks remain provisional and cannot be passed to
  release-check via `--fast`.

## Progress

- Source and focused contract regressions implemented.
- Focused boundary set: 7 passed.
- Initial combined focused run: 32 passed, 1 failed because the new helper
  rejected `PathLike` values in its test seam; widened to the same path-like
  inputs accepted by Python filesystem APIs and reran the boundary set green.
- Broad pytest initially exposed both the pre-existing 582-line
  `quality_runner/fleet/audit.py` and a new 569-line
  `quality_runner/task_prevention.py` failure. Extracted task-check projection
  and persistence into `task_check_projection.py`; task prevention is now 494
  lines. The targeted rerun passes all 40 task checks and leaves only the
  pre-existing fleet audit boundary.
- Final focused task/docs suite: 41 passed.
- Repository-wide Ruff, format, BasedPyright, environment contract, Vulture,
  pip-audit, Gitleaks, and package build pass.
- Final immutable-base validation task:
  `20260827T154124Z-b7fa8ab8-task-start`.
- Final authoritative release-check:
  `20260827T154224Z-c7a0c282-task-release-check`.
  It reports required enforcement, zero new enforced findings, zero unknown
  findings, unchanged toolchain/evidence/repository identity, and only
  `required_certified_gates_passed: false`. Its pytest gate has 1248 passing
  tests and the one pre-existing 582-line
  `quality_runner/fleet/audit.py` failure.
