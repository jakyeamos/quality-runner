# Subsystem foundation

Ownership and preservation constraints were accepted on 2026-09-09. See [the decision](decisions/subsystem-ratification-20260909.md), `development.json` revision 2, and the completed bootstrap quiz. Prior draft state is retained in `history/reconciliation-20260909/`.

Audit collection, separate task verification, review handoff, and consumer contracts are accepted boundaries. QR owns evidence meaning and freshness; consumers own presentation and live readback.

Accepted ownership is distinct from current proof. Root maturity is unchanged. Exact path bindings establish only their declared scope; unmapped paths, missing behavior inventories, admission obligations, and consumer readback remain explicit.

Use the canonical helper `family`, `change-context`, and `prove` interfaces. Preserve the entry packet and compare the actual diff at completion; reconcile scope expansion before preparing a replacement packet. Evidence receipts are local observations, not independent release approval.
