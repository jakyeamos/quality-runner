# Quality Runner object index

All entries are `status: stub` until a human-reviewed citation pass.

- `audit-evidence-engine` — `quality_runner/audit.py`,
  `quality_runner/audit_evidence.py`, `quality_runner/application/read_only_audit.py`,
  and `quality_evidence_contract/`; `universe: live`; does not execute
  remediation.
- `review-handoff-output` — review projection/reporting/response modules and
  integration-boundary docs; `universe: live`; does not own evidence
  collection.
- `consumer-contracts` — public adapter fixtures and integration docs;
  `universe: live`; does not redefine audit semantics.
- `repo-quality-certifier` — `repo_quality_certifier/`; `universe: unknown`;
  placement remains an unanswered boundary question.
- `support-and-evidence-artifacts` — artifacts, packaging, scripts, fixtures,
  and tests; `universe: live`; does not automatically become a child Compass.

