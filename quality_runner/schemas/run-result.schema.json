{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://quality-runner.dev/schemas/run-result.schema.json",
  "title": "Quality Runner Run Result",
  "type": "object",
  "required": ["schema", "status", "implementation_allowed", "run_id", "artifact_paths", "warnings"],
  "properties": {
    "schema": { "const": "quality-runner-run-result-v0.1" },
    "status": { "enum": ["clean", "planned"] },
    "implementation_allowed": { "const": false },
    "run_id": { "type": "string", "minLength": 1 },
    "artifact_paths": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },
    "analysis_coverage": { "$ref": "#/$defs/analysisCoverage" },
    "warnings": { "$ref": "#/$defs/warnings" }
  },
  "$defs": {
    "analysisCoverage": {
      "type": "object",
      "required": ["status", "deferred_checks", "scan_budget_exceeded", "performance_budget_exceeded"],
      "properties": {
        "status": { "enum": ["complete", "partial"] },
        "deferred_checks": { "type": "array", "items": { "type": "object" } },
        "scan_budget_exceeded": { "type": "boolean" },
        "performance_budget_exceeded": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "warnings": {
      "type": "array",
      "items": { "$ref": "#/$defs/warning" }
    },
    "warning": {
      "type": "object",
      "required": ["code", "message", "path"],
      "properties": {
        "code": { "type": "string", "minLength": 1 },
        "message": { "type": "string", "minLength": 1 },
        "path": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    }
  }
}
